<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body>

    <header class="bg-white shadow rounded-lg mx-4 mt-4 p-4 border border-gray-200">
        <div class="flex items-center justify-between">
            <div class="text-lg font-bold text-gray-700">Dashboard</div>
            <nav class="flex space-x-4">
                <a href="#" class="text-sm text-gray-600 hover:underline">Home</a>
                <a href="#" class="text-sm text-gray-600 hover:underline">Settings</a>
                <a href="#" class="text-sm text-gray-600 hover:underline">Logout</a>
            </nav>
        </div>
    </header>

    <div class="flex mt-4 mx-4">
        <!-- Sidebar -->
        <aside class="bg-white shadow rounded-lg p-4 border border-gray-200 w-64">
            <div class="text-lg font-bold text-gray-700 border-b border-gray-200 pb-2">Menu</div>
            <nav class="mt-4">
                <ul class="space-y-2">
                    <li>
                        <a href="#" class="block px-4 py-2 rounded hover:bg-gray-50 text-gray-600">Dashboard</a>
                    </li>
                    <li>
                        <a href="#" class="block px-4 py-2 rounded hover:bg-gray-50 text-gray-600">Profile</a>
                    </li>
                    <li>
                        <a href="#" class="block px-4 py-2 rounded hover:bg-gray-50 text-gray-600">Reports</a>
                    </li>
                </ul>
            </nav>
        </aside>

{#        {% include 'left_bar.html.twig' %}#}

            {% block body %}
            {% endblock %}


    </div>
    </body>
</html>
